[phases.setup]
nixPkgs = ["nodejs_20", "bun"]

[phases.install]
cmds = [
  "npm ci --prefer-offline --no-audit",
  "cd server && bun install --frozen-lockfile"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "./start.sh"
