{
  "name": "whisper-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd web && npm install && cd server && bun install",
    "build": "cd web && npm run build",
    "start": "cd web/server && bun --env-file=../.env index.ts",
    "dev": "cd web && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
